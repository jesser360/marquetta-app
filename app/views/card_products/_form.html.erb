<%= form_with(model: card_product, local: true) do |form| %>
  <% if card_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card_product.errors.count, "error") %> prohibited this card_product from being saved:</h2>

      <ul>
      <% card_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.label 'Name'%>
    <%= form.text_field :name, class: "form-control ", placeholder: 'Name of Card Product'%>
    <%= form.label 'Start Date'%>
    <%= form.text_field :start_date, class: "form-control ", placeholder: 'ex: 2017-10-05'%>
    <%= form.label 'Active?'%>
    <%= form.check_box :active, class: "form-control ", label: 'Active?'%><br>
    <%= form.submit %>

  </div>
<% end %>
