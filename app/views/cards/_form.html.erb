<%= form_with(model: card, local: true) do |form| %>
  <% if card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <label>Choose a User</label>
    <%= select_tag "user_id", options_from_collection_for_select(@users, "id", 'last_name'), class: "form-control required" %>
    <br>
    <label>Choose a Card Product Template</label>
    <%= select_tag "card_product_id", options_from_collection_for_select(@card_products, "id", "name"), class: "form-control required" %>
    <%= form.submit %>
  </div>
<% end %>
